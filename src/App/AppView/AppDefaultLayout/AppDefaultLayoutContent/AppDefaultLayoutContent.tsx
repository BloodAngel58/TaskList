import * as React from "react";
import AppRouterSwitch from "../../../AppRouterSwitch/AppRouterSwitch";

const AppDefaultLayoutContent: React.FC = () => {
  return (
      <AppRouterSwitch/>
  )
};

export default AppDefaultLayoutContent;
